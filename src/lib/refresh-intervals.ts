/** Dashboard charts + call log polling while the page is open. */
export const DASHBOARD_REFRESH_MS = 8_000;

/** How often the background worker / cron runs the auto-dialer queue. */
export const DIALER_POLL_MS = 5_000;
